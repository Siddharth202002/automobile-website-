# Reference design

The original AutoVera demo, kept for design comparisons.

- `AutoVera — Pre-Owned Car Dealership (Demo).pdf` — the source file.
- `01-…` to `11-…` — its pages exported as PNGs, one per screen.
  (`06` was a crop of the chat bubble and is not kept.)

These are inputs, not assets. Nothing in `src/` imports from this folder.
